## 1. CoreDNS ECS 输入处理

- [x] 1.1 在 CoreDNS 请求入口补充 ECS 解析逻辑，统一产出 `effectiveClientIP`（ECS 优先，失败则回退来源 IP）。
- [x] 1.2 为 ECS 解析结果增加上下文透传字段（含来源类型：ecs/fallback）供后续调度使用。
- [x] 1.3 补充 ECS 解析单元测试，覆盖有效 IPv4、缺失 ECS、非法 ECS、IPv6 前缀场景。

## 2. IP 到地市映射能力

- [x] 2.1 在 GoEdge 调度链路接入 `effectiveClientIP`，调用现有 IP 库解析 cityId/cityName。
- [x] 2.2 定义映射失败与空城市的标准返回结构，避免调度阶段分支不一致。
- [x] 2.3 增加映射测试样例（含 `61.142.56.193/32 -> 广州市`）与城市未命中场景。

## 3. 城市优先节点调度与降级

- [x] 3.1 在节点筛选阶段实现“城市优先”候选集过滤，仅选择健康可用节点。
- [x] 3.2 实现可配置降级策略（城市无节点时回退省级/全局既有策略）。
- [x] 3.3 补充端到端调度测试，验证城市命中与降级路径均返回有效 DNS 结果。

## 4. 配置与可观测性

- [x] 4.1 增加 ECS 城市路由配置开关及必要参数，并保持默认关闭。
- [x] 4.2 增加日志/指标埋点：ECS 命中、城市命中、降级原因、解析失败原因。
- [x] 4.3 在管理端或运维文档中补充开关说明、灰度步骤与回滚指引。

## 5. 联调与发布准备

- [ ] 5.1 在测试环境联调上游 ECS 透传，验证典型地域样本命中效果与时延变化。
- [x] 5.2 执行回归测试，确认无 ECS 流量行为与原有策略一致。
- [x] 5.3 制定生产灰度计划（小流量 -> 分批 -> 全量）并定义观测告警阈值。
